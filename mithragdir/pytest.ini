[pytest]
testpaths = gndlf-workflow/tests gndlf-pipeline/tests gndlf-core/tests
asyncio_mode = auto
filterwarnings = ignore::DeprecationWarning
addopts = -s
markers =
	requires_http: test performs live HTTP calls and can be skipped when Tolkien Gateway probe is non-200
