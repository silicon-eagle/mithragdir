[project]
name = "lembas-core"
version = "0.1.0"
description = "Core library for Lembas Service"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic",
    "loguru",
    "peewee>=3.17.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
