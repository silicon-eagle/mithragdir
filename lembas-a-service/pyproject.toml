[tool.uv.workspace]
members = ["cirdan", "gwaihir", "lembas-core"]

[project]
name = "lembas-service"
version = "0.1.0"
description = "Monorepo for Lembas Service"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
